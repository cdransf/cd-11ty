---
permalink: '/feed.json'
---
{% assign posts = collections.posts | reverse %}
{
  "posts": [
    {% for item in posts %}
      {
        "title": "{{ item.data.title }}",
        "url": "{{ item.url }}"
      }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
